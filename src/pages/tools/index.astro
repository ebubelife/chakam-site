---
// Only one tool exists today (see src/data/tools.ts), so /tools/ itself
// just forwards straight into it rather than showing an empty landing
// page. Once a second tool exists, worth reconsidering whether this
// should become a real picker screen instead.
return Astro.redirect('/tools/chat-report/');
---
